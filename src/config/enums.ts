export enum Gender {
  male = 0,
  female = 1,
  others = 2,
}

export enum Symptoms {
  fever = 0,
  stomachache = 1,
  headache = 2,
}

export enum AppointmentStatus {
  notFinished = 0,
  finished = 1,
}

export enum UserType {
  user,
  doctor,
}

export enum AppointmentRate {
  VeryBad = 0,
  Bad = 1,
  Ok = 2,
  Good = 3,
  Excellent = 4,
}
